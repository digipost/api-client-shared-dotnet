language: csharp
solution: api-client-shared.sln
mono: none
dotnet: 2.0.0
before_install:
  - sudo apt-get update
  - sudo apt-get install mono-complete=5.4.1
  - ls -la
  - wget https://dist.nuget.org/win-x86-commandline/latest/nuget.exe
  - mono nuget.exe
  - chmod +x ./nuget.exe
  - mono --version
install:
  - dotnet restore  
script:
  - ls -la
  - dotnet build -c Release
  #- dotnet test api-client-shared-tests/api-client-shared-tests.csproj
  - ls -la
  - mono nuget.exe pack api-client-shared.nuspec -Verbosity detailed
#deploy:
#  skip_cleanup: true
#  provider: script
#  script:
#    - nuget pack ./api-client-shared/api-client-shared.nuspec -Verbosity detailed
#  on:
#    branch: project-rider-test-netstandard
